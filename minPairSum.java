import java.util.Arrays;

public class minPairSum {
    public int minPairSum(int[] nums) {
        Arrays.sort(nums);
        int maxCount=0;
        int left=0;
        int right=nums.length-1;
        
    }
}
